<!--pages/imitate/school/major/major.wxml-->
<view class="page">
    <view class="page__bd">
      <block wx:for="{{major}}" wx:key="">
      <view class="weui-cells weui-cells_after-title" bindtap="major" id="{{item.DIC_ID}}" >
            <view class="weui-cell">
                <view class="weui-cell__bd ">{{item.NAME}}</view>
                <view class="weui-cell__ft" wx:if="{{item.checked}}">√</view>
                <view class="weui-cell__ft" wx:if="{{!item.checked}}"></view>
            </view>
        </view>
    </block>
    </view>
    
</view>